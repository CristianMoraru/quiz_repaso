'use strict';

module.exports = function(Quiz) {
/*
	Quiz.beforeRemote( asocia, function( ctx, next) {
	    //...
	    var token = app.models.accessToken;
	    if (token) {

	    }
	    next();
	});
*/
};
